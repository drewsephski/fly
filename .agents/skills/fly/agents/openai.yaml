interface:
  display_name: "Fly"
  short_description: "Repo-specific patterns and workflows for fly"
  default_prompt: "Use the fly repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true